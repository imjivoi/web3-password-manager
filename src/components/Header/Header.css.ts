import { style } from '@vanilla-extract/css'

const header = style({
  width: '100%',
  height: '60px',
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'center',
})

export { header }
